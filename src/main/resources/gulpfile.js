//Drar in gulp paketet från node_modules
var gulp = require('gulp');

//Sätter upp en gulp task man kan köra. Default är kommandot 'gulp' annars är det man skriver där kommandot
//man skriver efter gulp.
gulp.task('default', function() {
    // place code for your default task here
});